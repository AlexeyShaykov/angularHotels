import { CustomSplicePipe } from './custom-splice.pipe';

describe('CustomSplicePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSplicePipe();
    expect(pipe).toBeTruthy();
  });
});
